// Audio utility disabled per request to remove clicking sounds

export function playSynthBeep(_type?: 'success' | 'click' | 'warning' | 'connect') {
  // Audio playback disabled
}

export function playSuccessMp3() {
  // Audio playback disabled
}

